spring:
  application:
    name: todo-app
  datasource:
    url: jdbc:h2:mem:mytask
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    spring.jpa.database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: create
server:
  port: 9080
  servlet:
    context-path: /poli/api/v1

